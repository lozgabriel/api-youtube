import { ApiYoutubePipePipe } from './api-youtube-pipe.pipe';

describe('ApiYoutubePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ApiYoutubePipePipe();
    expect(pipe).toBeTruthy();
  });
});
